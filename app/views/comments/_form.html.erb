<%= form_for([@article, @article.comments.build]) do |f| %>
    <p>
      <%= f.label :"Ingrese su nombre" %><br>
      <%= f.text_field :user , placeholder: "Usuario"%>
    </p>
    <p>
      <%= f.label :"Ingrese el comentario" %><br>
      <%= f.text_area :body, placeholcer: "Ingresa tu texto aqui" %>
    </p>
    <p>
      <%= f.submit 'Comentar' %>
    </p>
  <% end %>